import React from 'react'
import { View, Text } from 'react-native'

// todo: make cool styling for errors :)
const Error = ({ children }) => {
  return <View>{children}</View>
}

export default Error
